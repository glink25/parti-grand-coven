{
  "name": "parti-grand-coven",
  "version": "0.2.0",
  "private": false,
  "type": "module",
  "description": "Grand Coven - an original simultaneous role-selection board game for Parti",
  "scripts": {
    "typecheck": "node --check src/worker/room.worker.js && node --check src/ui/client.js",
    "test": "node --test tests/*.test.mjs",
    "build": "npm run typecheck && node scripts/build.mjs",
    "package": "npm run build && cd dist && zip -qrFS ../parti.room.zip .",
    "check:package": "node scripts/check-package.mjs",
    "verify": "npm test && npm run build && npm run package && npm run check:package"
  }
}
